После дебага программы обнаружил:

1) Нужно оставить только одну проверку if (text.length() > 1) для символов до и после text.charAt(0) text.charAt(1) потому что делать две проверки для текущего и следующего символа не оптимально (text.length() > 0) (text.length() > 1)

2) Заменить мой метод isVowel на метод интерфейса Set contains() потому что мой метод isVowel() делал то же самое только был вынесен во вне

3) Вынести проверку на гласность символе после из if (!isCurrentSymbol && isVowel(symbolAfter, vowels)) в отдельную логическую переменную  boolean isSymbolAfter = vowels.contains(symbolAfter); для улучшения читаемости кода

4) убрать лишний условный оператор if который ничего не делает потому что вызовы до него итак формируют строку и это действие не имеет смысла
//               if (text.charAt(text.length() - 1) == currentSymbol) {
//                    return currentSymbol + "";
//                }









